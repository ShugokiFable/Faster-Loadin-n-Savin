name: CI

on:
  push:
    branches: [latest]
    tags: ['v*']
  pull_request:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up LDC
        uses: dlang-community/setup-dlang@v2
        with:
          compiler: ldc-1.42.0

      - name: Set up MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Build x86-64-v2
        shell: pwsh
        run: ./build.ps1 -BuildTag x86-64-v2 -TargetCPU x86-64-v2

      - name: Build x86-64-v3
        shell: pwsh
        run: ./build.ps1 -BuildTag x86-64-v3 -TargetCPU x86-64-v3

      - name: Package
        shell: pwsh
        run: ./package-release.ps1

      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: Faster-Loadin-and-Savin-package
          path: package/release/*

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download package
        uses: actions/download-artifact@v4
        with:
          name: Faster-Loadin-and-Savin-package
          path: pkg

      - name: Attach to release
        uses: softprops/action-gh-release@v2
        with:
          files: pkg/*
